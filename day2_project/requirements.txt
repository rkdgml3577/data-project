# ==============================================================================
# [Day 2 종합 실습] NYC Yellow Taxi 분석 파이프라인 의존성
# 광주캠퍼스 4반 5조
#
# 설치:  pip install -r requirements.txt
# 실행:  python 광주_4반_5조_day2종합실습.py
#
# Python 3.10 이상 필요
#   - X | Y 형태의 타입힌트(PEP 604)와 list[str] 표기를 사용하므로
#     3.9 이하에서는 TypeError 로 실행이 중단된다.
# ==============================================================================

# --- 데이터 처리 ---------------------------------------------------------------
pandas>=2.0          # DataFrame 연산, parquet 로딩, 기술통계
polars>=0.20         # Lazy scan_parquet — 로딩 성능 비교 실험
pyarrow>=14.0        # parquet 엔진 (pandas.read_parquet 이 내부적으로 사용)
numpy>=1.24          # 수치 연산, 상관계수, 무한대/결측 처리

# --- 시각화 -------------------------------------------------------------------
matplotlib>=3.7      # 정적 차트 렌더링, Agg 백엔드로 PNG 저장
seaborn>=0.12        # 히스토그램·히트맵 등 통계 차트
plotly>=5.18         # 인터랙티브 차트 → HTML 저장

# --- 통계 · 머신러닝 -------------------------------------------------------------
scipy>=1.10          # ttest_ind 등 통계 검정
scikit-learn>=1.3    # Pipeline, ColumnTransformer, RandomForest, f1_score
                     # 주의: import 이름은 sklearn 이지만 설치명은 scikit-learn
joblib>=1.3          # 학습된 Pipeline 직렬화 저장·재로딩

# --- 선택 의존성 ---------------------------------------------------------------
# tabulate 는 report.md 의 표를 마크다운 형식으로 렌더링할 때만 쓰인다.
# 미설치 시 to_table() 헬퍼가 to_string() 으로 자동 대체하므로 실행에는
# 지장이 없다. 보고서를 표 형태로 보고 싶다면 주석을 해제한다.
# tabulate>=0.9