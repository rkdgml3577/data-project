# [Day 1 종합실습] 의존 패키지 — venv 활성화 후: pip install -r requirements.txt
httpx>=0.27        # 비동기 HTTP 클라이언트 (asyncio 수집)
pydantic>=2.7      # v2 스키마 검증
pandas>=2.2        # DataFrame·CSV 입출력
pyarrow>=16.0      # Parquet 엔진
pytest>=8.0        # 스키마 검증 테스트
ruff>=0.4          # 코드 스타일 검사
